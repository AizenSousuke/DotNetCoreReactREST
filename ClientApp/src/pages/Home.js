import React from "react";
import { useSelector } from "react-redux";
import Layout from "../components/Layout";

const Home = () => {
  return (
    <Layout>
   Home Page!
    </Layout>
  );
};

export default Home;
